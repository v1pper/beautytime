{
  "build": {
    "builder": "NIXPACKS", 
    "buildCommand": "pip install Django Pillow gunicorn && pip install -r requirements.txt"
  },
  "deploy": {
      "startCommand": " && python manage.py migrate && echo \"from django.contrib.auth import get_user_model; User = get_user_model(); User.objects.create_superuser('admin', 'admin@example.com', '12345')\" | python manage.py shell && python manage.py runserver 0.0.0.0:$PORT"
  }
}