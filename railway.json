{
  "build": {
    "builder": "NIXPACKS", 
    "buildCommand": "pip install Django Pillow gunicorn && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd uslugi/project && python manage.py migrate && python manage.py runserver 0.0.0.0:$PORT"
  }
}